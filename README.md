# Final Project (Transport / ODPT)

## Setup
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

## Structure
- src/: scripts
- data/: sqlite db (ignored)
- notebooks/: analysis
- tests/: pytest
